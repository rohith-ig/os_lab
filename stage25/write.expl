int main() {
    decl    
        int a,temp,i;
        str b;
    enddecl
    begin
    write("Enter filename : ");
    read(b);
    a = exposcall("Create",b,0);
    a = exposcall("Open",b);
    i = 0;
    while (i < 2000) do
        temp = exposcall("Write",a,"word");
        i = i + 1;
    endwhile;
    if (temp == 0) then
        write("write done");
    else
        write("write fail");
        write(temp);
    endif;
    return 0;
    end
}