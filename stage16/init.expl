decl
        int a,temp,i,arr[3];
    enddecl
int main() {
    begin
        temp = exposcall("Write",-2,"enter som");
        i = 0;
        while (i<3) do
            temp = exposcall("Read",-1,arr[i]);
            i = i + 1;
        endwhile;
        i = 0;
        while (i<3) do
            temp = exposcall("Write",-2,arr[i]);
            i = i + 1;
        endwhile;
        temp = exposcall("Read",-1,a);
        temp = exposcall("Write",-2,a);
        return 0;
    end
}